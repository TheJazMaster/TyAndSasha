using Nickel;

namespace TheJazMaster.TyAndSasha;

internal interface ITyCard
{
	static abstract void Register(IModHelper helper);
}

internal interface ITyArtifact
{
	static abstract void Register(IModHelper helper);
}